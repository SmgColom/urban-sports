# URL del endpoint GraphQL del backend.
# En local suele ser http://localhost:4000/graphql
# En produccion, la URL de tu backend desplegado (Render, Railway, etc.)
VITE_GRAPHQL_URI=http://localhost:4000/graphql
